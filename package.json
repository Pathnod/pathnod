{
  "name": "pathnod",
  "version": "0.1.0",
  "private": true,
  "description": "Pathnod monorepo: privacy-preserving observations of physical infrastructure, built on Solana.",
  "license": "Apache-2.0",
  "packageManager": "npm@11.19.0",
  "workspaces": [
    "packages/verifier"
  ],
  "engines": {
    "node": "24.21.0",
    "npm": "11.19.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
